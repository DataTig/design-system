import { Meta } from "@storybook/blocks";

<Meta title="Home" />


CSS Class names can be seperated with _ if wanted. (This is used over - so in IDE's you can double click and highlight the whole rule name in one go.)

